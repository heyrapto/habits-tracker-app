import { StyleSheet, Text, View } from "react-native";

export default function StreaksScreen() {
    return (
        <View>
            <Text style={{color: "red"}}>Hello, this is streaks page</Text>
        </View>
    );
}

const styles = StyleSheet.create({})